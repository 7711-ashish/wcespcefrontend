import { create } from "apisauce";

const apiClient = create({
    // baseURL: "https://resource-tracker.onrender.com/api"
    baseURL : "https://render-wce-space.onrender.com/api"
});

export default apiClient;   
